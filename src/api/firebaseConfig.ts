// api/firebase.ts
import auth from '@react-native-firebase/auth';
import app from '@react-native-firebase/app';

// No need for manual initialization since google-services.json handles this
export {auth, app};
